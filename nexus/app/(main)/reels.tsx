import React from 'react';
import { View, Text } from 'react-native';

const ReelsScreen = () => {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="text-lg">Reels Screen</Text>
    </View>
  );
};

export default ReelsScreen;
